<div class="best-seller_card">
	<a href="{{ path('product', { 'slug' : product.slug }) }}">
        <div class="best-seller_card_img_product" style="background-image: url('/uploads/{{ product.image }}'); 
    background-position: center;
    background-repeat: no-repeat;
    height: 425px;
    width: 300px;
    position: relative;
    background-size: cover;">
            <div class="best-seller_card_content_product">
                <div>
                    <h5 class="product_name">{{ product.name }}</h5>
                    <span class="product_price">A partir de {{ (product.price / 100)|number_format(2, ',', '.') }}€</span>
                </div>
                <i class="fa-solid fa-eye"></i>
            </div>
        </div>
	</a>
</div>
